{
  "tests": [
    {
      "name": "GET without token returns 403",
      "method": "GET",
      "path": "/",
      "expectedStatus": 403,
      "expectedBody": {"error": "Нет доступа — войдите как учитель"},
      "bodyMatcher": "partial"
    },
    {
      "name": "POST without token returns 403",
      "method": "POST",
      "path": "/",
      "body": {"name": "Иван"},
      "expectedStatus": 403,
      "bodyMatcher": "partial"
    }
  ]
}
